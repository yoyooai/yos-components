{
  "name": "yos-channels",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "test": "node --test --test-reporter=tap test/*.test.mjs",
    "verify": "node scripts/verify.mjs"
  }
}
