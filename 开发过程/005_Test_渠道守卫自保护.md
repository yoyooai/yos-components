# 渠道仓守卫自保护记录

## 背景

飞书候选的运行时权限修复和包卫生门禁已经存在，但缺少能证明这些保护不可被无声撤掉的自动测试。
同时，组件测试可以被 `skip` 或 `only` 关闭而根门禁继续报绿。

本轮只改门禁与测试，不修改飞书或微信的收发消息、凭据、C4、生命周期和版本兼容行为。

## 实现

1. 根门禁扫描全部 Git 跟踪测试，拒绝 `skip`、`todo`、`only`、`xit`、`xdescribe` 和 Jest 路径忽略配置。
2. 飞书运行时测试真实启动 `src/index.js`，证明既存 `0755` 数据目录会被收紧到 `0700`。
3. 飞书包门禁使用临时 Git 仓库，分别证明未跟踪文件和 `/Users/...` 私人路径会被拒绝。
4. 根 `verify` 改为运行完整 `npm test`，确保新增根级门禁测试不是孤立文件。
5. 微信候选 rebase 到新的飞书守卫基线；微信组件目录及其专属门禁相对原锁定提交逐字未变。

## 开发侧验证

飞书守卫分支：

- 根测试 `12/12`
- 飞书测试 `29/29`
- 飞书审计 0 漏洞
- 飞书包 27 项，内容摘要 `a23b395215882cca9f25d2e0ba717e90a34401aa9ac4e0fea810d1419536ef6f`

同步后的微信候选：

- 根测试 `18/18`
- 飞书测试 `29/29`
- 微信测试 `14/14`
- 两个组件审计均为 0 漏洞
- 飞书包 27 项；微信包 29 项
- 微信包内容摘要 `de2dea940c1f671c3e11122dba4aa1faa882130b4ca74a1ff5ce9e26e2a202e1`

## 撤销验证

以下行为均实际报红：

- 删除飞书运行时 `chmod(DATA_DIR, 0o700)`
- 把运行时权限改回 `0755`
- 往飞书包加入未跟踪文件
- 往飞书包植入 `/Users/...` 私人路径
- 把飞书组件测试改成 skip

## 未完成

- 小A尚未对本轮守卫和同步后的微信候选做独立复验。
- 本轮未合 `main`、未打标签、未登记 Registry、未部署服务器。
- 仓内守卫不能替代 GitHub 分支保护和作者之外的独立验收。

## 微信计数与关键边界补强

2026-08-04 将微信测试从普通退出码步骤提升为独立计数组：

- 根仓批准底线 29，开发侧最终执行 `29/29`。
- 飞书批准底线 29，开发侧最终执行 `29/29`。
- 微信批准底线 14，开发侧最终执行 `14/14`。
- 微信协议测试、仓库合同、独立包门禁和上游来源记录进入关键文件清单。
- 微信 API 直接请求层新增完整来源负测，拒绝 HTTP、凭据、query、fragment 和伪造子域名，并确保在网络请求前失败。
- 微信包门禁保持独立执行，不依赖 `prepack` 是否被调用。

开发侧撤销验证均实际报红：把微信测试缩到 4 条、删除关键协议测试、把 API 主机检查改为全放行、移除独立包门禁。收官后的微信包为 29 项，内容摘要为 `06a6cd097a91884abdeb9d924b37dc3014be62780d5f0f3d135803a5c9900f9c`。

## 收官补强

- 测试执行函数必须返回每组实际通过数；返回空值时，在审计和打包前失败。
- 把测试调用包进 warning-only `try/catch` 时，静态接线守卫明确拒绝。
- 微信真实 PM2 启动测试进入关键文件清单，删除后门禁失败。
- 微信数据目录从 `0755` 放宽回归时，真实 PM2 测试失败。

开发侧最终门禁：根仓 `29/29`、飞书 `29/29`、微信 `14/14`，两个组件依赖审计均 0 漏洞，`npm run verify` PASS。本节不替代小A独立复验。

## 外层吞错与最终 validator 收口

- 根仓批准底线提升为 30，开发侧执行 30/30；飞书 29/29、微信 14/14。
- 测试执行与计数 validator 返回显式确认；确认未被最终消费时，审计和打包步骤前失败。
- test gate 抛异常时整体验证非零；确认状态和最终判决位于验证 try/catch 外，
  内层 warning-only 包装不能再由后续步骤覆盖成 PASS。
- 静态守卫单独检查最终 validator 行、gate 调用、状态声明和最终判断；
  删除任一处，或把声明/判断移回 try/catch 内，都会报红。
- 最终 npm run verify PASS，两个组件审计均 0 漏洞；飞书包摘要保持
  6f0fcc4390e3c131169902a682f60ef935a06b26d46015cb5eb48556fc294444，
  微信包摘要保持
  06a6cd097a91884abdeb9d924b37dc3014be62780d5f0f3d135803a5c9900f9c。

以上仍为开发侧证据，不替代小A独立复验。

## executed-test 只交回数据

2026-08-05 按同一门禁原则把渠道仓的执行门禁收口成“只交回数据，不交回结论”：

- `executeTestGate` 只返回根仓、飞书、微信各自的原始执行计数。
- `runVerification` 在主验证 `try/catch` 外统一调用 `verifyRecordedTestCountsImpl(counts, testSuites, approvedBaselines)`，之后才允许进入包校验与审计步骤。
- `undefined`、`true`、空值、错误对象或 warning-only 包装后的伪成功都在打包前失败。
- 静态守卫同步要求原始计数返回、外层 validator、状态声明和执行顺序都不能被移除或移回 `try/catch` 内。
- 后续审计或包校验命令启动失败时，验证器返回失败并保留受控错误信息，不再抛出 Node 裸调用栈。
- 根仓批准底线同步提升为当前实测的 `32/32`。此后每次净新增测试都必须同步提高批准底线并更新摘要，不能让新增护栏落在旧底线之外。

开发侧最终复跑：`test/verify-test-policy-wiring.test.mjs` 与 `test/test-policy.test.mjs` 共 `13/13` 通过。
这仍属于开发侧证据，不替代小A独立复验。
